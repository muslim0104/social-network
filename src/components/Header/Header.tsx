import React from "react";
import classes from './Header.module.css'
export const Header=()=>{
    return(
        <header className={`${classes.active}  ${classes.header}`} >

        </header>
    )
}